from itertools import permutations
from itertools import combinations

arr = ['A', 'B', 'C']
permutations_list = list(permutations(arr, 2))
combinations_list = list(combinations(arr, 2))

print(permutations_list)
print(cobinations_list)
